# Backend API base URL. Leave unset in development to use the Vite proxy (/api -> localhost:8000).
# For production builds point it at your deployed backend, e.g. https://api.example.com/api
VITE_API_URL=http://localhost:8000/api
